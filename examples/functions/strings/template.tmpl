{{ "Hello" | upper }} !

{{ concat ((replace "GoodBye World" "GoodBye" "Hello") | upper) " !" }}

{{ replace "this is a string with spaces replaced by plus" " " "+" }}

If you want to slugify a string, just use `slugify` function ({{"the sLug @ result0 ;)"|slugify}})